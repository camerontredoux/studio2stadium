{{{
  exports({
    to: routes.path
  })
}}}
import router from "@adonisjs/core/services/router";

router
  .group(() => {})
  .prefix("{{ routes.name }}")
  .openapi({ tags: ["{{ routes.name }}"] });
