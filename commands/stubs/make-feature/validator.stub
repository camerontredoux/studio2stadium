{{{
  exports({
    to: validator.path
  })
}}}
import vine from "@vinejs/vine";
import { Infer } from "@vinejs/vine/types";

export const {{ validator.name }} = vine.compile(vine.object({}));

export type {{ validator.name }} = Infer<typeof {{ validator.name }}>;
