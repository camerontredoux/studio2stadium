{
  "private": "true",
  "type": "module",
  "packageManager": "pnpm@10.19.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
